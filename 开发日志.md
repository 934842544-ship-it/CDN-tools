# 开发日志

## 2026-07-03 客户出账数据可视化管理系统

### 1. 需求
实现客户出账数据可视化管理系统：支持批量上传月度出账Excel文件，自动筛选 `属性16 == '异网标品'` 的数据，按"发生月份"覆盖入库（重复上传同月份先删后插），提供汇总页（表格+分组柱状图）与详情页（日历+下拉+50行分页），并支持汇总页单元格点击跳转到详情页对应筛选结果。

### 2. 碰到问题
1. Excel列名为中文，需与数据库英文字段做映射；月份字段可能为datetime或字符串，需统一为 `YYYY-MM` 格式。
2. 覆盖逻辑要求事务原子性：同月份先删后插，若中途失败需回滚避免数据丢失。
3. 详情页URL含中文市公司参数，前端跳转需 `encodeURIComponent` 编码，后端Flask自动解码。
4. 测试脚本中 urllib 对中文URL需 `urllib.parse.urlencode` 编码，直接拼接会触发 ascii 编码错误。
5. 汇总页表格需同时支持横向滚动与首列/首行冻结，便于查看大数据量矩阵。

### 3. 解决方式
1. 在 [config.py](file:///c:/Users/19927/Documents/trae_projects/CDN-tools/config.py) 集中维护 `EXCEL_COLUMN_MAP` 列名映射；用 `pandas.to_datetime(errors="coerce")` 规整月份，失败回退原字符串。
2. [excel_parser.py](file:///c:/Users/19927/Documents/trae_projects/CDN-tools/excel_parser.py) 的 `import_records` 使用 `BEGIN/commit/rollback` 显式事务，按 `occurrence_month` 分组先删后插。
3. 前端 [summary.js](file:///c:/Users/19927/Documents/trae_projects/CDN-tools/static/js/summary.js) 单元格点击时 `encodeURIComponent(city)` 拼接跳转URL。
4. 测试改用 `urllib.parse.urlencode` 构造查询串。
5. CSS 中 `position:sticky` 实现首行首列冻结 + `.table-wrap` 滚动容器。

### 4. 需求实现方式
- **技术栈**：Flask 3.x + SQLite + pandas/openpyxl + ECharts 5.x（CDN）。
- **目录结构**：
  - 后端：[config.py](file:///c:/Users/19927/Documents/trae_projects/CDN-tools/config.py)（集中配置）、[db.py](file:///c:/Users/19927/Documents/trae_projects/CDN-tools/db.py)（连接+建表）、[excel_parser.py](file:///c:/Users/19927/Documents/trae_projects/CDN-tools/excel_parser.py)（解析+筛选+覆盖入库）、[app.py](file:///c:/Users/19927/Documents/trae_projects/CDN-tools/app.py)（路由+API）。
  - 前端：[templates/](file:///c:/Users/19927/Documents/trae_projects/CDN-tools/templates) 三个页面 + [static/js/](file:///c:/Users/19927/Documents/trae_projects/CDN-tools/static/js) 三个脚本 + [static/css/style.css](file:///c:/Users/19927/Documents/trae_projects/CDN-tools/static/css/style.css)。
- **数据上传**：[POST /upload](file:///c:/Users/19927/Documents/trae_projects/CDN-tools/app.py#L49) 接收多文件 → 临时保存 → pandas解析 → 过滤异网标品 → 按月份事务覆盖入库 → 删除临时文件。
- **汇总页**：[GET /api/summary](file:///c:/Users/19927/Documents/trae_projects/CDN-tools/app.py#L69) 返回 `cities/months/matrix`，前端渲染表格（行=市公司，列=月份）+ ECharts分组柱状图（X=市公司，系列=月份），单元格点击跳 `/detail?city=&month=`。
- **详情页**：[GET /api/detail](file:///c:/Users/19927/Documents/trae_projects/CDN-tools/app.py#L92) 支持 `city/month/page` 参数，`city=全部` 时不限制城市，`LIMIT 50 OFFSET` 后端分页；前端 `<input type="month">` 日历 + 市公司下拉（含"全部"）+ 分页器。
- **金额格式化**：前端 `toLocaleString` 保留两位小数带千分位。

### 5. 启动方式
```
pip install -r requirements.txt
python app.py
```
访问 http://127.0.0.1:5000

### 6. 验证结果
- 上传5行测试数据（含1行非"异网标品"）→ 正确过滤为4行入库。
- 汇总矩阵金额正确（南京2025-01=2000.50=1200.5+800）。
- 重传同月份文件 → 总数保持4行，覆盖逻辑生效。
- 详情页支持按市公司+月份筛选、全部查看、分页正常。
- 所有页面路由与API返回200。
